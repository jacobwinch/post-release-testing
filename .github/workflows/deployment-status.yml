name: Test PR events

on:
  pull_request: 
    types: [labelled, synchronize]

jobs:
  hello-world:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
        - name: test
          run: |
              echo ${{ toJSON(github.event) }}
