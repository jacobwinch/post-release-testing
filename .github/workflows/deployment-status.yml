name: Test PR events

on:
  pull_request: 
    types: [labeled, synchronize]

jobs:
  hello-world:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
        - name: test
          run: |
              echo ${{ toJSON(github.event) }}
