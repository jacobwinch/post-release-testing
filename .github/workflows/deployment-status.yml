name: Test PR events

on:
  pull_request: 
    types: [labeled]

jobs:
  hello-world:
    if: ${{ github.event.label.id == 2340250864 }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
        - name: test
          run: |
              echo ${{ toJSON(github.event) }}
